.pure-u-1-3
.pure-u-1-3
  #clearance.password-reset
    %h2= t(".title")
    %p= t(".description")
    = form_for :password, url: passwords_path, html: { class: 'pure-form pure-form-stacked' } do |form|
      .text-field
        = form.label :email
        = form.text_field :email, type: 'email'
      .submit-field
        = form.submit class: 'pure-button button-secondary'
