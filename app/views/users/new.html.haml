.pure-u-1-3
.pure-u-1-3
  #clearance.sign-up
    = form_for @user, html: { class: 'pure-form pure-form-stacked' } do |form|
      %fieldset
        %legend.white-text= t("users.new.title")
        = render partial: '/users/form', object: form
        .submit-field
          = form.submit class: 'pure-button button-secondary'
        .other-links
          = link_to t(".sign_in"), sign_in_path
